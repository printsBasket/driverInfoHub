# Driver Info Hub

Next.js 15 App Router TypeScript project scaffold for Driver Info Hub.

Setup:

```bash
npm install
npm run dev
```

Environment:

Create a `.env.local` with:

```
EMAIL_HOST=
EMAIL_PORT=
EMAIL_USER=
EMAIL_PASS=
```
