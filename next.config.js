/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["images.unsplash.com", "placehold.co", "www.driverinfohub.com", "driverinfohub.com"],
  }
}

module.exports = nextConfig
